<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.insigma.ordercenter.mapper.TagMapper">

    <select id="getTagList" resultType="com.insigma.ordercenter.entity.vo.TagVO">
    SELECT
        a.product_id,
        a.tag_id,
        a.tag AS tagCode,
        b.data_value AS tag
    FROM
        t_tag a
    LEFT JOIN sys_dictionary b ON a.tag = b.data_code
    AND b.parent_id = 21
    WHERE a.product_id=#{productId}
    </select>
</mapper>
